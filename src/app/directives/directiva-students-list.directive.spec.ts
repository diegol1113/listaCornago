import { DirectivaStudentsListDirective } from './directiva-students-list.directive';

describe('DirectivaStudentsListDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaStudentsListDirective();
    expect(directive).toBeTruthy();
  });
});
